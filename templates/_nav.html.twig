<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_static_home') }}">{{
      'navigation.brand' | trans
    }}</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a
            href="{{ path('app_static_home') }}"
            class="nav-link {{
              app.current_route == 'app_static_home' ? 'active' : ''
            }}"
            >{{ 'navigation.home' | trans }}</a
          >
        </li>
        <li class="nav-item">
          <a
            href="{{ path('app_weather_highlander', { threshold: 11 }) }}"
            class="nav-link {{
              app.current_route == 'app_weather_highlander' ? 'active' : ''
            }}"
            >{{ 'navigation.highlander_says' | trans }}</a
          >
        </li>
        <li class="nav-item">
          <a
            href="{{ path('app_privacy_policy') }}"
            class="nav-link {{
              app.current_route == 'app_privacy_policy' ? 'active' : ''
            }}"
            >{{ 'navigation.pivacy_policy' | trans }}</a
          >
        </li>
      </ul>
    </div>
  </div>
</nav>
